# Yamanaka memory examples

Examples for minimal Obsidian-compatible memory notes used by v2.0.0.

Memory notes are Markdown files with YAML-style frontmatter. They are evidence indexes, not authority.
