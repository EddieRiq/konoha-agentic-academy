# UI examples

This directory contains examples for the Local Web UI Alpha.

The Local Web UI Alpha is local-only, adds no new authority, and keeps Git operations, repository apply, real model invocation, private context access, and autonomous execution blocked.
