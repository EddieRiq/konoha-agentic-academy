# General Task Execution Workbench examples

These examples are public-safe and generic.

They demonstrate:

- a task workbench plan;
- command batches;
- a command result evidence record;
- a workbench review report.

Command proposals are not permission.

v2.8 does not execute arbitrary commands. Results may be recorded only as evidence after supervised or external execution.
