# Model Router and Token Economy examples

These examples show model runtime profiling, routing decisions, local model download planning, and token usage ledger structure.

They are evidence examples only. They do not invoke models, download models, execute commands, or grant runtime authority.
