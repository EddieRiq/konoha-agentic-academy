# Controlled Tool Execution examples

This folder contains public examples for the Controlled Tool Execution Gate.

The example plan validates a Mission Workspace through an allowlisted internal Konoha tool. It is not permission to execute arbitrary shell commands, use network access, invoke models, invoke adapters, perform Git operations, apply repository changes, access private context, or close missions.
