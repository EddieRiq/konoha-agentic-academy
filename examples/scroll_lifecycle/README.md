# Scroll lifecycle examples

This directory contains generic, public examples for Scroll learning proposal workflows.

The examples are evidence only. They do not rewrite doctrine, modify official Scroll definitions, authorize execution, authorize model calls, authorize adapter calls, authorize repository apply, authorize Git operations, authorize private context access, or close missions.
