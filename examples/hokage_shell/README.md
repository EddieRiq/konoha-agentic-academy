# Hokage Terminal Shell examples

This folder contains example outputs for the terminal-first Hokage Shell.

The shell is not a permission system. It displays mission state, asks for explicit approval, records local evidence, and writes private Obsidian-compatible memory notes when configured to a local ignored memory root.
