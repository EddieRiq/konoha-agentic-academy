# Notification examples

This directory contains examples for mission notification state and UI state escalation.

Notification state is evidence only. It does not authorize execution, model calls, adapter calls, repository apply, Git operations, private context access, or mission closure.
