# Examples

Public, generic examples for Konoha Agentic Academy.

Examples are documentation-first. They do not execute commands, mutate files, access private context, or authorize runtime actions.

## Dry-run missions

- `dry_run_missions/`: public dry-run mission packages showing mission intake, dry-run planning, validation, trace logging, package assembly, and closure.
