# Asset resolver examples

These examples show how Konoha resolves logical visual assets.

Resolution order:

```text
local_village -> user -> public_generic -> text_fallback
```

Local and user asset paths should remain private. Public examples must use generic, original, license-safe assets only.
